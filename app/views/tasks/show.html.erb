<div class="container">
  <div class ="row">
        <div class="col">
          <table class="table">

          <% @task.users.pluck(:name).each do  |name| %>
          <tr><td><%= name %> </td> </tr>
          <% end %>
          </table>
        </div>

     <div class="w-100" style="margin-top:20px"></div>

  </div>
</div>
